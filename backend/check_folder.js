// ไฟล์: backend/check_folder.js
const fs = require('fs');
const path = require('path');

console.log("\n🕵️‍♂️ === เริ่มปฏิบัติการตามหาโฟลเดอร์ Models ===");
console.log("📍 จุดที่คุณยืนอยู่ตอนนี้ (Root):", __dirname);

// 1. เช็คว่ามีโฟลเดอร์ models อยู่หน้าบ้านไหม?
const path1 = path.join(__dirname, 'models');
if (fs.existsSync(path1)) {
    console.log("✅ เจอ! โฟลเดอร์ 'models' อยู่ถูกที่แล้ว (backend/models)");
    console.log("   📄 ไฟล์ข้างในมี:", fs.readdirSync(path1));
} else {
    console.log("❌ ไม่เจอ! โฟลเดอร์ 'models' ไม่ได้อยู่หน้าบ้าน");
}

// 2. แอบเช็คว่าเผลอเอาไปซ่อนไว้ใน controllers หรือเปล่า?
const path2 = path.join(__dirname, 'controllers', 'models');
if (fs.existsSync(path2)) {
    console.log("⚠️ เจอ! แต่อยู่ผิดที่! มันซ่อนอยู่ใน 'backend/controllers/models'");
    console.log("   👉 วิธีแก้: ลากโฟลเดอร์ models ออกมาไว้ข้างนอกครับ");
}

// 3. แอบเช็คว่าชื่อผิดไหม (เช่น Model ไม่มี s)
const path3 = path.join(__dirname, 'model');
if (fs.existsSync(path3)) {
    console.log("⚠️ เจอ! แต่ชื่อผิด! คุณตั้งชื่อว่า 'model' (ไม่มี s)");
    console.log("   👉 วิธีแก้: เปลี่ยนชื่อโฟลเดอร์เป็น 'models'");
}

console.log("============================================\n");