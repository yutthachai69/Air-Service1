# 1. โฟลเดอร์ Library ที่หนักมาก (ไม่ต้องเก็บ เพราะสั่ง npm install ใหม่ได้)
node_modules/

# 2. ความลับ ห้ามหลุดเด็ดขาด! 🚨
.env
firebase-key.json

# 3. ฐานข้อมูล (เก็บไว้ที่เครื่องใครเครื่องมัน ไม่ทับกัน)
database.sqlite
*.db
*.sqlite
*.sqlite3

# 4. ไฟล์ที่ User อัปโหลดมา (ไม่ควรเอาขึ้น Git)
uploads/

# 5. ไฟล์ขยะของระบบปฏิบัติการ
.DS_Store
Thumbs.db

# 6. Log ไฟล์ต่างๆ
logs/
*.log
npm-debug.log*